<!-- Generated by `go generate ./...` (internal/coveragegen). Do not edit by hand. -->
# DataFactory

Azure's `datafactory` service · portable interface `driver.Factories` · [Azure index](./README.md)

## Operations (6)

| Operation | Description |
| --- | --- |
| `CreateOrUpdateFactory` | CreateOrUpdateFactory stores the factory as a full replace and reports |
| `DeleteFactory` | DeleteFactory removes the factory, returning NotFound if it does not exist. |
| `GetFactory` | GetFactory returns the factory identified by (resourceGroup, name), or |
| `ListFactories` | ListFactories returns every factory in the subscription, ordered by ID. |
| `ListFactoriesByResourceGroup` | ListFactoriesByResourceGroup returns the factories in resourceGroup, ordered |
| `UpdateFactory` | UpdateFactory applies a PATCH (Factories_Update): it REPLACES tags and |

## Not in scope

_Not documented yet. See the [emulator boundary](../../../README.md) for cloudemu-wide non-goals._
