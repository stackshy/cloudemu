<!-- Generated by `go generate ./...` (internal/coveragegen). Do not edit by hand. -->
# Bastion

Azure's `bastion` service · portable interface `driver.BastionHosts` · [Azure index](./README.md)

## Operations (4)

| Operation | Description |
| --- | --- |
| `CreateOrUpdateBastionHost` | CreateOrUpdateBastionHost stores host as a full replace and reports whether |
| `DeleteBastionHost` | DeleteBastionHost removes the host, returning NotFound if it does not exist. |
| `GetBastionHost` | GetBastionHost returns the host identified by (resourceGroup, name), or |
| `ListBastionHosts` | ListBastionHosts returns the hosts in rg, or all when rg is empty |

## Not in scope

_Not documented yet. See the [emulator boundary](../../../README.md) for cloudemu-wide non-goals._
