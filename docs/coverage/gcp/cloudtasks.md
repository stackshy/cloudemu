<!-- Generated by `go generate ./...` (internal/coveragegen). Do not edit by hand. -->
# CloudTasks

GCP's `cloudtasks` service · portable interface `driver.Queues` · [GCP index](./README.md)

## Operations (11)

| Operation | Description |
| --- | --- |
| `CreateQueue` | CreateQueue stores a queue. cfg.Name is the full resource name; the queue |
| `DeleteQueue` | DeleteQueue removes a queue by its full resource name. |
| `GetIamPolicy` | GetIamPolicy returns the queue's stored IAM policy (an empty, versioned |
| `GetQueue` | GetQueue returns a queue by its full resource name. |
| `ListQueues` | ListQueues returns every queue under the given parent |
| `PatchQueue` | PatchQueue applies a field-mask update to an existing queue and returns it. |
| `PauseQueue` | PauseQueue transitions a queue to PAUSED and returns it. |
| `PurgeQueue` | PurgeQueue purges a queue's tasks. Tasks are out of scope, so this records |
| `ResumeQueue` | ResumeQueue transitions a queue to RUNNING and returns it. |
| `SetIamPolicy` | SetIamPolicy stores the queue's IAM policy and returns it with a refreshed |
| `TestIamPermissions` | TestIamPermissions echoes back the requested permissions (CloudEmu does not |

## Not in scope

_Not documented yet. See the [emulator boundary](../../../README.md) for cloudemu-wide non-goals._
