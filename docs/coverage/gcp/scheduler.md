<!-- Generated by `go generate ./...` (internal/coveragegen). Do not edit by hand. -->
# Scheduler

GCP's `scheduler` service · portable interface `driver.Scheduler` · [GCP index](./README.md)

## Operations (8)

| Operation | Description |
| --- | --- |
| `CreateJob` | CreateJob stores a job. cfg.Name is the full resource name; the job is |
| `DeleteJob` | DeleteJob removes a job by its full resource name. |
| `GetJob` | GetJob returns a job by its full resource name. |
| `ListJobs` | ListJobs returns every job under the given parent |
| `PatchJob` | PatchJob applies a field-mask update to an existing job and returns it. |
| `PauseJob` | PauseJob transitions a job to PAUSED and returns it. |
| `ResumeJob` | ResumeJob transitions a job to ENABLED and returns it. |
| `RunJob` | RunJob force-runs a job now. Dispatch is out of scope, so this records the |

## Not in scope

_Not documented yet. See the [emulator boundary](../../../README.md) for cloudemu-wide non-goals._
