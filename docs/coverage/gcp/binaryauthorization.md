<!-- Generated by `go generate ./...` (internal/coveragegen). Do not edit by hand. -->
# BinaryAuthorization

GCP's `binaryauthorization` service · portable interface `driver.BinaryAuthorization` · [GCP index](./README.md)

## Operations (10)

| Operation | Description |
| --- | --- |
| `CreateAttestor` | CreateAttestor stores a new attestor under projects/{project}/attestors/ |
| `DeleteAttestor` | DeleteAttestor removes an attestor by its full resource name. |
| `GetAttestor` | GetAttestor returns an attestor by its full resource name. |
| `GetIamPolicy` | GetIamPolicy returns the attestor's stored IAM policy (an empty, versioned |
| `GetPolicy` | GetPolicy returns the project's policy. A project that has never set a |
| `ListAttestors` | ListAttestors returns every attestor under the given project, in |
| `SetIamPolicy` | SetIamPolicy stores the attestor's IAM policy and returns it with a |
| `TestIamPermissions` | TestIamPermissions echoes back the requested permissions (CloudEmu does not |
| `UpdateAttestor` | UpdateAttestor fully replaces the mutable fields of an existing attestor |
| `UpdatePolicy` | UpdatePolicy fully replaces the project's policy and returns it. The policy |

## Not in scope

_Not documented yet. See the [emulator boundary](../../../README.md) for cloudemu-wide non-goals._
