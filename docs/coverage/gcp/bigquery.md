<!-- Generated by `go generate ./...` (internal/coveragegen). Do not edit by hand. -->
# BigQuery

GCP's `bigquery` service · portable interface `driver.BigQuery` · [GCP index](./README.md)

## Operations (12)

| Operation | Description |
| --- | --- |
| `DeleteDataset` | DeleteDataset removes the dataset. When deleteContents is false and the |
| `DeleteTable` | DeleteTable removes the table, or NotFound. |
| `GetDataset` | GetDataset returns the dataset, or NotFound. |
| `GetTable` | GetTable returns the table, or NotFound. |
| `InsertDataset` | InsertDataset creates a dataset under project. It returns AlreadyExists |
| `InsertTable` | InsertTable creates a table under project/datasetID. It returns |
| `ListDatasets` | ListDatasets returns every dataset in project, ordered by datasetId. |
| `ListTables` | ListTables returns every table in the dataset, ordered by tableId. |
| `PatchDataset` | PatchDataset merges the supplied fields of patch into the dataset (the |
| `PatchTable` | PatchTable merges the supplied fields of patch into the table (labels are |
| `UpdateDataset` | UpdateDataset replaces the dataset's mutable fields with the supplied |
| `UpdateTable` | UpdateTable replaces the table's mutable fields with the supplied patch. |

## Not in scope

_Not documented yet. See the [emulator boundary](../../../README.md) for cloudemu-wide non-goals._
