package cloudemu

// Regenerate the capability coverage docs (docs/coverage/) from the driver
// interfaces and provider factories. Run `go generate ./...` after changing a
// driver interface or wiring a new service into a provider.
//go:generate go run ./internal/coveragegen
